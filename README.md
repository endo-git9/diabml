# DiabML — Diabetes Ensemble Framework (research-grade)

How to run:
1. Create virtualenv and install deps:
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
